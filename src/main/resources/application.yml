spring:
  application:
    name: liveklass-assignment
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/liveklass}
    username: ${DB_USERNAME:liveklass}
    password: ${DB_PASSWORD:liveklass}
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        jakarta:
          persistence:
            lock:
              timeout: 5000
  sql:
    init:
      mode: never

enrollment:
  cancel:
    window-days: 7

logging:
  level:
    org.hibernate.SQL: debug
